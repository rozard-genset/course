module:
    mounts:

    - source: "langua"
      target: "i18n"

    - source: "library"
      target: "layouts/partials/lib/course"
    
    - source: "format/model"
      target: "layouts/partials/format/course"

    - source: "format/script"
      target: "assets/scripts/format/course"

    - source: "markit"
      target: "layouts/course/_markup"
    
    - source: "engine/model"
      target: "layouts/partials/course/module"

    - source: "engine/script"
      target: "assets/scripts/course/module"

    - source: "blocks/model"
      target: "layouts/partials/blocks/course"

    - source: "blocks/script"
      target: "assets/scripts/blocks/course"

    - source: "assets"
      target: "assets/media/course"